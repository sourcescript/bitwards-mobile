'use strict';

/**
 * @ngdoc function
 * @name bitwardsApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Controller of the bitwardsApp
 */
angular.module('bitwardsApp')
  .controller('LoginCtrl', function ($scope) {
   	angular.element("body").attr('class','login');
  });
